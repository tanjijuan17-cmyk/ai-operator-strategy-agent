graph LR
    A[开始<br/>接收用户输入的运营需求] --> B[需求解析<br/>gpt-4o-mini<br/>把用户的自然语言需求转成结构化运营需求]
    B --> C[代码执行<br/>接收需求解析节点输出的结构化数据，根据预设的同义词映射表进行检索词扩展与去重，生成优化后的知识检索查询语句retrieval_query，同时透传can_generate判断结果至条件分支节点]
    C --> D{条件分支<br/>IF 代码执行result是True OR 代码执行result不是False<br/>ELSE<br/>基于代码执行节点输出的can_generate布尔值进行逻辑判断}
    D -->|True| E[知识检索<br/>运营方法论<br/>瑞幸营销案例<br/>利用代码执行节点生成的优化检索词retrieval_query，在瑞幸咖啡内部运营知识库中进行语义匹配，检索出相关的运营案例、策略方法论等参考材料]
    D -->|False| F[信息追问<br/>Pro/moonshot<br/>当用户输入信息不足时，向用户发起针对性追问，引导其补充缺失的关键运营要素如目标人群、场景等]
    E --> G[策略生成<br/>Pro/moonshot<br/>结合用户原始需求、结构化运营要素以及检索到的行业知识，生成符合瑞幸运营体系的、具备可落地性的运营策略建议]
    F --> H[直接回复<br/>回复信息追问结果]
    G --> I[直接回复2<br/>回复策略生成结果]
    
